# Domain for your app
DOMAIN=your-domain.com

# Evolution API key (generate random string)
EVOLUTION_API_KEY=your-secret-api-key-here

# PostgreSQL (change in production!)
POSTGRES_USER=wa_user
POSTGRES_PASSWORD=wa_password
POSTGRES_DB=wa_database
